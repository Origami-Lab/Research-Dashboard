import React from 'react';
import styled from 'styled-components';
import { Input, Icon } from 'antd';

const HeaderWrapper = styled.div`
  width: 200px;
  height: 100%;
  overflow: auto;
`;

function Header() {
  return (
    <HeaderWrapper className="scrollbar">
     
    </HeaderWrapper>
  );
}

export default Header;
